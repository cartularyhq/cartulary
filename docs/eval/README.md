# Evaluation documentation

Reserved for deterministic PR-gate notes, nightly/release evaluation plans, and
benchmark replay documentation.

The initial evaluation architecture is defined by `AD-EVAL-*` in
`blueprint/memory-system-architecture-and-nfr.md`. Future task PRs should add
concrete scenarios, commands, fixtures, and reporting formats here only when the
corresponding implementation exists.
