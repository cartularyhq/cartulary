# GitHub Codex prompts

Reserved for repository-scoped GitHub/Codex prompt files.

The current local Codex helper prompts live under `.codex/prompts/`. Add files
here only when GitHub-side Codex automation is enabled and a task defines the
prompt name, trigger, and required permissions.
