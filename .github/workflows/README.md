# GitHub Actions workflows

Reserved for CI and automation workflows.

Do not add an active workflow here until the corresponding task specifies the
checks, secrets, permissions, and branch protection expectations. The L3
automation plan should introduce workflows one PR at a time so each permission
change remains reviewable.
